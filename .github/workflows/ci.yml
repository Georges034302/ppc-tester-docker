name: Analyze Vowel Frequency
on: [push]

jobs:
  analyze:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v2

      - name: Build and Run Docker Container
        uses: docker://docker://<your_docker_image>
        with:
          args: data.txt
